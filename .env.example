# ========================================
# 환경 설정 (Environment Configuration)
# ========================================

# Node 환경 (development, production, test)
NODE_ENV=development

# ========================================
# 서버 설정 (Server Configuration)
# ========================================

# 서버 포트 (1-65535)
PORT=3001

# 바인딩 호스트
# - localhost: 로컬만 접근 가능
# - 0.0.0.0: 외부 접근 허용 (Docker 환경)
HOST=0.0.0.0

# ========================================
# OBS WebSocket 설정 (필수)
# ========================================

# OBS WebSocket 호스트
# - localhost: 로컬 OBS
# - host.docker.internal: Docker에서 호스트 OBS 연결
OBS_WS_HOST=localhost

# OBS WebSocket 포트
OBS_WS_PORT=4455

# OBS WebSocket 비밀번호 (필수!)
# OBS Studio > 도구 > WebSocket 서버 설정에서 확인
OBS_WS_PASSWORD=your_obs_password_here

# ========================================
# GitHub 연동 설정 (필수)
# ========================================

# GitHub Webhook 비밀키 (필수!)
# GitHub Repository > Settings > Webhooks에서 설정
GITHUB_WEBHOOK_SECRET=your_webhook_secret_here

# GitHub 사용자명
GITHUB_USERNAME=garimto81

# ========================================
# YouTube API (라이브 제목 동기화)
# ========================================

# YouTube Data API v3 키 (필수)
# Google Cloud Console > APIs & Services > Credentials에서 발급
# YouTube Data API v3 활성화 필요
YOUTUBE_API_KEY=

# YouTube 채널 ID (권장 - 라이브 자동 검색)
# 채널에서 현재 진행 중인 라이브를 자동으로 찾아 제목 동기화
# 채널 URL: https://www.youtube.com/channel/UC9WM0_WZyDC7e8y-vfR99tw
#                                          └────────────────────────┘
#                                          이 부분이 CHANNEL_ID
YOUTUBE_CHANNEL_ID=

# YouTube 비디오 ID (선택 - 특정 영상 지정)
# VIDEO_ID가 설정되면 CHANNEL_ID보다 우선 적용
# 단, 매 방송마다 ID가 바뀌므로 일반적으로 불필요
# YOUTUBE_VIDEO_ID=

# ========================================
# YouTube 제목 동기화 동작 방식
# ========================================
# 1. 세션 시작 시 YouTube API로 현재 라이브 제목 조회
# 2. 5분마다 제목 변경 확인 후 오버레이 업데이트
# 3. 제목이 변경된 경우에만 WebSocket 브로드캐스트
